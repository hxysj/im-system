info:
  contact: {}
paths:
  /index:
    get:
      responses:
        "200":
          description: OK
          schema:
            type: string
      tags:
      - 首页
  /user/deleteUser:
    get:
      parameters:
      - description: 用户id
        in: query
        name: id
        type: string
      responses:
        "200":
          description: code","message"}
          schema:
            type: string
      tags:
      - 用户模块
  /user/getUserList:
    get:
      responses:
        "200":
          description: code","message"}
          schema:
            type: string
      tags:
      - 用户模块
  /user/login:
    post:
      parameters:
      - description: 用户名称
        in: formData
        name: name
        type: string
      - description: 密码
        in: formData
        name: password
        type: string
      responses:
        "200":
          description: code","message","data"}
          schema:
            type: ""
      tags:
      - 用户模块
  /user/register:
    post:
      parameters:
      - description: 用户名
        in: formData
        name: name
        type: string
      - description: 密码
        in: formData
        name: password
        type: string
      - description: 确认密码
        in: formData
        name: repassword
        type: string
      responses:
        "200":
          description: code","message"}
          schema:
            type: string
      tags:
      - 用户模块
  /user/update:
    post:
      parameters:
      - description: 用户id
        in: formData
        name: id
        type: string
      - description: 名称
        in: formData
        name: name
        type: string
      - description: 密码
        in: formData
        name: password
        type: string
      - description: 手机号
        in: formData
        name: phone
        type: string
      - description: 邮箱
        in: formData
        name: email
        type: string
      - description: 性别
        in: formData
        name: identity
        type: string
      responses:
        "200":
          description: code","message"}
          schema:
            type: string
      tags:
      - 用户模块
swagger: "2.0"
